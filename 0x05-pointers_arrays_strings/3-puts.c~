#include "main.h"

/**
 *_puts - A function that prints a new string
 *@str: The string variable
 *Return: Void
 */

void _put(char *str)
{
	int j;

	for (j = 0; str[j] != '\0'; i++)
	{
		_putchar(str[i]);
	}
	_putchar('\n');
}
