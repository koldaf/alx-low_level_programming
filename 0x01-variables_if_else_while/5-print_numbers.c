#include <stdio.h>

/**
 *main - Another function to return
 *Return: numbers from 0 - 9
 */

int main(void)
{
	int y;

	for (y = 0; y <= 9; y++)
	{
		printf("%d", y);
	}
	putchar('\n');
	return (0);
}
