#include <stdio.h>
/**
 *main - a C program to print with proper grammr... text
 *Return: 0 if exited properly
 */
int main(void)
{
  printf("with proper grammar, but the outcome is a piece of art,");
  return (0);
}

